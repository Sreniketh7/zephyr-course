if (CONFIG_OUR_DRIVER)
    zephyr_library()
    zephyr_library_sources(our_driver.c)
    add_dependencies(..__app__drivers__our_driver zephyr_generated_headers)
    if(CONFIG_SHELL)
        target_sources(app PRIVATE our_driver_shell.c)
    endif()
endif()